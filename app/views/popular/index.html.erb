<h1>Popular URLs</h1>
<p></p>

  <table>
  <tr>
    <td>Url</td>
    <td>Views</td>
  <tr>
  <% @urls.each do |url| %>
    <tr>
      <td><%= url.url %></td>
      <td><%= url.url_views %></td>
    </tr>
  <% end %>
  </table>

<%= will_paginate @urls %>